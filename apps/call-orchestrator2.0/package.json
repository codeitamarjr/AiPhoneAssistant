{
  "name": "call-orchestrator2.0",
  "version": "1.0.1",
  "description": "A Fastify-based application for handling calls and interactions with OpenAI API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "server": "node --watch src/index.js",
    "tunnel": "ngrok http 5050 --log=stdout",
    "dev": "concurrently -k -s first -n server,ngrok -c blue,magenta \"npm:server\" \"npm:tunnel\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@fastify/formbody": "^8.0.0",
    "@fastify/websocket": "^11.0.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
